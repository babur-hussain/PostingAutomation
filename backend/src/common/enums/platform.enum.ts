/**
 * Shared platform enum used by both Posts and Social Accounts modules.
 * Consolidates the previously duplicated PostPlatform and SocialPlatform enums.
 */
export enum Platform {
  INSTAGRAM = 'instagram',
  FACEBOOK = 'facebook',
  YOUTUBE = 'youtube',
  X = 'x',
  THREADS = 'threads',
}
